name: k8s
description: Kubernetes client library for Dart. Maintained by Kubeec Team.
version: 1.27.0+dev.2
repository: https://github.com/kubeec/k8s-dart
issue_tracker: https://github.com/kubeec/k8s-dart/issues
topics: [kubernetes]

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  collection: ^1.18.0
  dio: "^5.4.0"
  json_annotation: ^4.8.1
  kubeconfig: ^1.0.3
  logging: ^1.2.0
  path: ^1.9.0
  socks5_proxy: ^1.0.4

dev_dependencies:
  build_runner: 2.4.8
  json_serializable: ^6.7.1
  test: ^1.25.2
  very_good_analysis: ^5.1.0

screenshots:
  - description: Kubernetes Client for Dart logo
    path: assets/logo.webp
