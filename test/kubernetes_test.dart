// import 'package:k8s/k8s.dart';
import 'package:test/test.dart';

void main() {
  group('Kubeconfig', () {
    // final kubernetes = Kubernetes();

    test('client can be instantiated with initDefault method', () async {
      // TODO(fatih): Mock
      // await kubernetes.initDefault();
      // expect(kubernetes.client, isNotNull);
      expect(true, isTrue);
    });
  });
}
